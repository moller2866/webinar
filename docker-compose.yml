services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    volumes:
      - blog-data:/app/data
    environment:
      - DB_PATH=/app/data/blog.db

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  blog-data:
